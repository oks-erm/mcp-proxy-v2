[tool.poetry]
name = "absence-mcp"
version = "0.1.0"
description = "MCP server for absence.io (read-only: users, absences via API v2 + Hawk)"
authors = ["Host-Wise"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
mcp = ">=1.26.0,<2.0.0"
pydantic = ">=2.0.0"
uvicorn = ">=0.27.0"
fastapi = ">=0.115.0"
google-auth = ">=2.23.0"
google-cloud-secret-manager = ">=2.20.0"
python-dotenv = ">=1.0.0"
requests = ">=2.28.0"
requests-hawk = ">=0.5.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
